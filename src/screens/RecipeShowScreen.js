import { StyleSheet, Text, View } from 'react-native';

const RecipeShowScreen = () => {
  return (
    <View>
      <Text>Recipe Show Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default RecipeShowScreen;
